import { StyleSheet, View, Text } from 'react-native';

const addCheckPoints: React.FC = () => {
    return (
        <View>
            <Text>Checkpoints</Text>
        </View>
    );
};

const styles = StyleSheet.create({
    container: {}
});

export default addCheckPoints;
